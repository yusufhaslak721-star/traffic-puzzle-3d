# Traffic Puzzle 3D

Mobile-first Three.js traffic puzzle prototype for KARAGAMESTUDIO.

Current active build uses the Claude-fixed route/camera files with import-cycle-safe wrappers so the menu and controls initialize correctly.

Build marker: module-cycle fix.
